scalar DateTime

type Query {
  users: [User]
  userById(id: ID): User
}

type User {
  id: ID
  email: String
  createdBy: String
  createdDate: DateTime
  lastModifiedBy: String
  lastModifiedDate: DateTime
}